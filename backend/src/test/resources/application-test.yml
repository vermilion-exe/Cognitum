spring:
  application:
    name: backend-test
  datasource:
    url: jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE;INIT=CREATE SCHEMA IF NOT EXISTS COGNITUM_DATA
    username: sa
    password: 
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: false
    properties:
      hibernate:
        jdbc:
          time_zone: UTC
        format_sql: true
    database: postgresql
  mail:
    host: localhost
    port: 587
    username: test@test.com
    password: testpassword
    protocol: smtp

application:
  security:
    jwt:
      secret-key: IlrUNIsxShbr4BMEpUDS9G8/ABr0EW/8GyN0MKeFpK8=
      jwt-expiration: 86400000
      refresh-token-expiration: 2419200000

app:
  summarizer:
    client:
      base-url: http://host.docker.internal:8000

nvidia:
  api:
    url: https://integrate.api.nvidia.com/v1
    model: meta/llama-3.3-70b-instruct
    key: test-key
